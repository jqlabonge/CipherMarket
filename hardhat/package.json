{
  "name": "black-box-bazaar-hardhat",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat project for BlackBoxBazaar: real Solidity test suite + Base Sepolia deploy script. Run on a machine with normal network access (the sandbox this was built in blocks the npm registry).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:baseSepolia": "hardhat run scripts/deploy.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  }
}
