The autonomous Buyer Agent (deterministic rule-based auto-purchasing) is
implemented in frontend/index.html under the "Autonomous Agents" tab rather
than as a separate script, so it can actually execute real transactions via
the user's connected wallet in the browser. See README.md section 8.
